generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root@localhost:3306/freelancer_platform"
}

model User {
  id    Int    @id @default(autoincrement())
  name  String
}
